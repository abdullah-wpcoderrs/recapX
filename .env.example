# X OAuth Configuration (Required)

# OAuth Client Credentials from X Developer Portal
X_CLIENT_ID=your_oauth_client_id_here
X_CLIENT_SECRET=your_oauth_client_secret_here

# OAuth Redirect URI (must match X app settings)
X_REDIRECT_URI=https://your-domain.com/api/auth/callback

# Note: All three variables are required for OAuth authentication
# Get these from https://developer.twitter.com/ → Your App → User authentication settings